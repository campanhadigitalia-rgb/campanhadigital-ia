import { WhatsappCommand } from '../../components/ui/WhatsappCommand';

export default function Messaging() {
  return (
    <div className="flex flex-col gap-6 w-full h-full">
      <WhatsappCommand />
    </div>
  );
}
