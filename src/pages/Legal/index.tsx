import { LegalOnboarding } from '../../components/ui/LegalOnboarding';

export default function LegalDocsPage() {
  return (
    <div className="flex flex-col gap-6 w-full animate-in fade-in duration-300 h-full">
      <LegalOnboarding />
    </div>
  );
}
