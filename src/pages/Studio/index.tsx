import { ContentStudio } from '../../components/ui/ContentStudio';

export default function Studio() {
  return (
    <div className="flex flex-col gap-6 w-full h-full">
      <ContentStudio />
    </div>
  );
}
